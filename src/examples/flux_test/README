This example by Chris D. demonstrates a flow which has 
node functions with very little processing.  It was a benchmark
of a sort at various times to measure performance of various
runtimes & hardware.
